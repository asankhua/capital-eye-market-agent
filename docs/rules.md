1. Always write tests for what ever code you are writing.
2. Always write logs for all functions/flows or anything implemented, make something like dump.log file for logs.
3. Always create mocks in tests, never make Gemini calls as well have very limited number of free tier calls.
4. Create/Update a makefile for all the common commands like install, dev, test, lint, docker-build etc
5. Use ./docs directory to create any markdown or artifacts